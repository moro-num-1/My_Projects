#include <stdio.h>
#include <cs50.h>

int main(void)
{
    string word = get_string("Input: ");
    for (int i = 0; word[i] != '\0'; i++)
    {
        printf("%c", word[i]);
    }
    printf("\n");
}
